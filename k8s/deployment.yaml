apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "80"
    prometheus.io/path: "/metrics"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-observability
  template:
    metadata:
      labels:
        app: app-observability
    spec:
      containers:
        - name: app
          image:  vinny6202/app-observability:v1
          ports:
          - name:  http
            containerPort:  8000
            protocol: TCP